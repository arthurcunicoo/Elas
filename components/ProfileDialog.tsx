import { Modal, Pressable, StyleSheet, Text, View } from 'react-native';
import { colors } from '../constants/theme';
import { TouchFeedback } from './TouchFeedback';
export type ProfileNotice = { title: string; message: string; confirm?: () => void };
export function ProfileDialog({ notice, onClose }: { notice: ProfileNotice | null; onClose: () => void }) {
  return <Modal visible={!!notice} transparent animationType="fade" onRequestClose={onClose}><View style={styles.backdrop}><Pressable accessibilityLabel="Fechar mensagem" onPress={onClose} style={StyleSheet.absoluteFill} /><View accessibilityViewIsModal style={styles.dialog}><Text style={styles.title}>{notice?.title}</Text><Text style={styles.message}>{notice?.message}</Text><View style={styles.actions}>{notice?.confirm && <TouchFeedback label="Cancelar" onPress={onClose} style={styles.button}><Text style={styles.action}>Cancelar</Text></TouchFeedback>}<TouchFeedback label={notice?.confirm ? 'Sair' : 'Entendi'} onPress={() => { const confirm = notice?.confirm; onClose(); confirm?.(); }} style={styles.button}><Text style={styles.action}>{notice?.confirm ? 'Sair' : 'Entendi'}</Text></TouchFeedback></View></View></View></Modal>;
}
const styles = StyleSheet.create({ backdrop: { flex: 1, backgroundColor: '#26091466', justifyContent: 'center', padding: 28 }, dialog: { backgroundColor: colors.white, borderRadius: 22, padding: 24, width: '100%', maxWidth: 400, alignSelf: 'center' }, title: { fontSize: 20, fontWeight: '700', color: colors.wine }, message: { marginTop: 12, lineHeight: 22, fontSize: 15, color: colors.wine }, actions: { flexDirection: 'row', justifyContent: 'flex-end', gap: 12, marginTop: 14 }, button: { minHeight: 44, paddingHorizontal: 12 }, action: { color: colors.pink, fontWeight: '700' } });
